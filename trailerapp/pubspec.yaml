name: trailerapp
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.2.0
  location: ^5.0.0
  geocoding: ^2.0.1
  http: any  # Esto usará la versión más reciente disponible

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Agregar assets aquí
  assets:
    - assets/images/penske.jpg
    - assets/images/loves.jpg
    - assets/images/ta.jpg
